// Fibonacci in Nova
//
// Demonstrates recursion and basic arithmetic.

fn fib(n: i64) -> i64 {
    if n <= 1 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

fn main() {
    let result = fib(10)
    print(result)  // Should print 55
}
